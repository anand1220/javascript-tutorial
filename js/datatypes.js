"use strict";  // treat all js code as a newer version

// number = 2;
// BigInt
// string = "";
let state = null;
// undefined
// symbol = unique
let boolean = true / false;
console.log(boolean);

// typeof operator 

console.log(typeof null); // object
console.log(typeof undefined);  // undefined
console.log(null == null);



/* 
## NaN stands for Not a Number. It is a special value in javascript.
- It is a result of an operation that csn not results in a meaningful numeric result.

isNaN() - used to check the value is a valid number or not. 

let result = "hellow" / 2;
console.log(result);  // NaN
console.log(isNaN(result));  // true
console.log(typeof NaN);  // number
*/


/*
## Infinity - It is a special numeric value that represents positive infinity. It is a global property which is a part of Number datatype.

## let value = -Infinity; // represent negative infinity

let value = Infinity;
console.log(5 * Infinity);  // Infinity
console.log(19 / Infinity);  // 0

*/




